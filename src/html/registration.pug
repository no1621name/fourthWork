section#registration.registration
  .registration-content.container(x-data="registration")
    .about-registration
      .about-registration-text
        h3.title #[b Запишитесь] бесплатно на консультацию или задайте вопрос
        .on-consultation
          span На консультации:
          .on-consultation-content
            .consultations-list
              template(x-for="(onConsultation, onConsultationInd) in onConsultationsList" x-key="onConsultationInd")
                .consultation(x-html="onConsultation")
            .registration-contacts
              .main-contacts
                span #[span.online-mark Звоните,] Пн-ВС с 8:00 до 21:00
                span.phone-number #[img(src="./img/icons/phone.svg" alt="phone-icon" loading="lazy")] 7 (499) 286 88 60
              .messengers-container
                span Или пишите, #[b мы онлайн]
                +messengers()
      img(src="./img/haritonow/haritonow2.png", alt="haritonow2" loading="lazy")
    form
      h4.form-title #[b Заполните форму,] и мы свяжемся с вами в течение 5 минут в рабочее время
      input(required type="text" name="client-name" placeholder="Как вас зовут?")
      input(
        required type="tel"
        name="client-phone-number"
        x-mask="+7 (999)999-99-99"
        pattern="\+7 \([0-9]{3}\)[0-9]{3}\-[0-9]{2}\-[0-9]{2}"
        placeholder="+7 (_ _ _) _ _ _ - _ _ - _ _"
      )
      label(for="clientConsult")
        select#clientConsult(name="client-consult" required @change="event.target.classList.add('changed')")
          option(value="" selected="" disabled) Выберите тип консультации
          template(x-for="(consultation, consultationInd) in consultationsList" x-key="consultationInd")
            option(value="consultation" x-text="consultation")
      button(type="submit") Записаться на бесплатную консультацию #[img(src="./img/icons/top-arrow.svg" alt="top-arrow" loading="lazy")]
      .special-terms
        input#acceptTerms(required type="checkbox" name="client-accept-terms")
        label(for="acceptTerms") #[a(href="/").underline-decorate Согласен с условиями обработки персональных данных]